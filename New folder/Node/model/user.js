const express = require("express");
const { create } = require("../controller/userController");
module.exports = {
    create: (req, res) => {
        try {
            
        } catch (error) {
            
        }
    }
};